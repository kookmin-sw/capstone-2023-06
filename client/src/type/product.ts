export type ProductData = {
  name: string;
  tags: string[];
  thumbnail: string;
  subThumbnail: string[];
  detail: string;
  price: string;
};

export type CommentData = {
  user: string,
  comment: string,
}